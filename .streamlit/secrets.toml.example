# Copy to secrets.toml for local dev, or set in Streamlit Cloud → Settings → Secrets.
# The app falls back to a hard-coded key if this is absent, but set it here for
# deployment so the key isn't committed.
NASS_API_KEY = "your-usda-nass-quickstats-key"
